# Gitleaks configuration — https://github.com/gitleaks/gitleaks#configuration
# NOTE: `paths` entries are Go RE2 regular expressions, NOT globs. A `**` glob here
# crashes gitleaks ("missing argument to repetition operator") and fails the scan.

title = "Actual Budget REST API"

[extend]
useDefault = true

[[allowlists]]
description = "Test fixtures, mocks and helpers"
paths = ['''^tests?/''']
